# Discord Bot Configuration
BOT_TOKEN=your_discord_bot_token_here
# Alternative token name (for compatibility)
DISCORD_TOKEN=your_discord_bot_token_here

# Optional: Redis for caching (will use in-memory fallback if not available)
REDIS_URL=redis://localhost:6379

# Dashboard Configuration
DASH_PORT=3000
ADMIN_USER=admin
ADMIN_PASS=supersecret

# Comma-separated IPv4/IPv6 whitelist for dashboard access
DASH_ALLOW_IPS=127.0.0.1,::1

# Optional: Specific Guild ID for faster command registration during development
# GUILD_ID=your_guild_id_here

# Google Cloud Speech API (for voice translation - optional)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json
